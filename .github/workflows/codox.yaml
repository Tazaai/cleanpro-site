name: Codox Diagnostics & Self-Healing
on: workflow_dispatch

jobs:
  diagnostics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Codox Review
        run: |
          chmod +x review_report.sh
          bash review_report.sh
